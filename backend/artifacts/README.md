# Model Artifacts

This directory contains trained model files used by the backend:

```text
cnn_fbank_best.pt
rf_mfcc.joblib
knn_mfcc.joblib
```

The AST checkpoint is stored in the backend root:

```text
../gtzan_ast_best.pt
```

These files are committed through Git LFS because they are binary model artifacts.
